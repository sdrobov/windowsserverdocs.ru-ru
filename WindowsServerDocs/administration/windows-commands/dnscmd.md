---
title: Dnscmd
description: 'Раздел Windows команды для ***- '
ms.custom: na
ms.prod: windows-server-threshold
ms.reviewer: na
ms.suite: na
ms.technology: manage-windows-commands
ms.tgt_pltfrm: na
ms.topic: article
ms.assetid: e7f31cb5-a426-4e25-b714-88712b8defd5
author: coreyp-at-msft
ms.author: coreyp
manager: dongill
ms.date: 10/16/2017
ms.openlocfilehash: 39478e9b7dd8e8c69ed07f5d431486a7ed96b9cb
ms.sourcegitcommit: 0d0b32c8986ba7db9536e0b8648d4ddf9b03e452
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/17/2019
ms.locfileid: "59815505"
---
# <a name="dnscmd"></a>Dnscmd

>Область применения. Windows Server (полугодовой канал), Windows Server 2016, Windows Server 2012 R2, Windows Server 2012

Интерфейс командной строки для управления DNS-серверами. Эта служебная программа используется в пакетных файлах для автоматизации повседневных задач управления DNS или для выполнения простой автоматической установки и настройки DNS-серверов в сети сценариев.  
## <a name="syntax"></a>Синтаксис  
```  
dnscmd <ServerName> <command> [<command parameters>]  
```  
## <a name="parameters"></a>Параметры  
|Параметр|Описание|  
|-------|--------|  
|<ServerName>|IP-адрес или имя удаленного или локального DNS-сервер.|  
## <a name="commands"></a>Команды  
|Command|Описание|  
|------|--------|  
|[dnscmd /ageallrecords](#BKMK_1)|Задает текущее время на всех меток времени в зоне или узла.|  
|[dnscmd/ClearCache](#BKMK_2)|Очищает кэш DNS-сервера.|  
|[dnscmd/Config](#BKMK_3)|Сбрасывает конфигурацию сервера или зоны DNS.|  
|[dnscmd /createbuiltindirectorypartitions](#BKMK_4)|Создает встроенные разделы каталога приложений DNS.|  
|[dnscmd /createdirectorypartition](#BKMK_5)|Создает раздел каталога приложений DNS.|  
|[dnscmd /deletedirectorypartition](#BKMK_6)|Удаляет раздел каталога приложений DNS.|  
|[dnscmd /directorypartitioninfo](#BKMK_7)|Выводит сведения о разделе каталога приложений DNS.|  
|[dnscmd /enlistdirectorypartition](#BKMK_8)|DNS-сервер добавляет к набору репликации раздела каталога приложений DNS.|  
|[dnscmd /enumdirectorypartitions](#BKMK_9)|Список разделов каталога приложений DNS для сервера.|  
|[dnscmd /enumrecords](#BKMK_10)|Возвращает список записей ресурсов в зоне.|  
|[dnscmd /enumzones](#BKMK_11)|Список зон, размещаемых на указанном сервере.|  
|[dnscmd /exportsettings](#BKMK_25a)|Записывает сведения о конфигурации сервера в текстовый файл.|  
|[dnscmd/Info](#BKMK_12)|Возвращает сведения о сервере.|  
|[dnscmd /ipvalidate](#BKMK_29a)|Проверяет удаленный DNS-серверов.|  
|[dnscmd /nodedelete](#BKMK_13)|Удаляет все записи для узла в зоне.|  
|[dnscmd/RecordAdd](#BKMK_14)|Добавляет запись ресурса зоны.|  
|[dnscmd /recorddelete](#BKMK_15)|Удаляет запись ресурса из зоны.|  
|[dnscmd /resetforwarders](#BKMK_16)|Задает DNS-серверы для переадресации рекурсивных запросов.|  
|[dnscmd /resetlistenaddresses](#BKMK_17)|Задает IP-адреса сервера для обслуживания запросов DNS.|  
|[dnscmd /startscavenging](#BKMK_18)|Инициирует очистку сервера.|  
|[dnscmd/STATISTICS](#BKMK_19)|Запрашивает, или очищает server статистических данных.|  
|[dnscmd /unenlistdirectorypartition](#BKMK_20)|Удаляет DNS-сервер из набора репликации раздела каталога приложений DNS.|  
|[dnscmd /writebackfiles](#BKMK_21)|Сохраняет все данные зоны или корневую ссылку в файл.|  
|[dnscmd /zoneadd](#BKMK_22)|Создает новую зону на DNS-сервере.|  
|[dnscmd /zonechangedirectorypartition](#BKMK_23)|изменяет данный раздел каталога, на котором размещаются зоны.|  
|[dnscmd /zonedelete](#BKMK_24)|Удаление зоны DNS-сервера.|  
|[dnscmd /zoneexport](#BKMK_25)|Записывает записи ресурсов зоны в текстовый файл.|  
|[dnscmd /zoneinfo](#BKMK_26)|Отображает сведения о зоне.|  
|[dnscmd /zonepause](#BKMK_27)|Приостанавливает зоны.|  
|[dnscmd /zoneprint](#BKMK_28)|Отображаются все записи в зоне.|  
|[dnscmd /zonerefresh](#BKMK_30)|вызывает обновление дополнительной зоны из основной зоны.|  
|[dnscmd /zonereload](#BKMK_31)|Перезагружает зоны из базы данных.|  
|[dnscmd /zoneresetmasters](#BKMK_32)|изменение главных серверов, которые предоставляют сведения о передачи зоны для дополнительной зоны.|  
|[dnscmd /zoneresetscavengeservers](#BKMK_33)|изменяет серверы, которые можно удалить зону.|  
|[dnscmd /zoneresetsecondaries](#BKMK_34)|Сбрасывает дополнительной информации для зоны.|  
|[dnscmd /zoneresettype](#BKMK_29)|Изменяет тип зоны.|  
|[dnscmd /zoneresume](#BKMK_35)|Возобновляет зоны.|  
|[dnscmd /zoneupdatefromds](#BKMK_36)|Обновляет зоны, интегрированной в active directory, используя данные из доменных служб active directory (AD DS).|  
|[dnscmd /zonewriteback](#BKMK_37)|Сохраняет данные зоны в файл.|  
### <a name="BKMK_1"></a>dnscmd /ageallrecords  
Задает текущее время на отметку времени для записей ресурсов в указанной зоны или узел на DNS-сервере.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /ageallrecords <ZoneName>[<NodeName>] | [/tree]|[/f]  
```  
#### <a name="parameters"></a>Параметры  
<ServerName>  
Указывает DNS-сервер, который администратор планирует управления, представленный IP-адрес, полное доменное имя (FQDN) или имя узла. Если этот параметр опущен, используется локальный сервер.  
<ZoneName>  
Указывает полное ДОМЕННОЕ имя зоны.  
<NodeName>  
Указывает конкретный узел или поддерево в зоне. *NodeName* указывает узел или поддерево в зону с помощью следующих:  
-   @ для корневая зона или полное доменное имя  
-   Полное ДОМЕННОЕ имя узла (имя с точки (.) в конце)  
-   Одну метку для имени относительно корня зоны  
/Tree  
Указывает, что все дочерние узлы также получать метку времени.  
/f  
Выполняет команду без запроса на подтверждение.  
#### <a name="remarks"></a>Примечания  
-   **Ageallrecords** команда является для обеспечения обратной совместимости между текущей версии службы DNS и предыдущих версиях DNS, в котором очистки не поддерживались. Он добавляет метку времени, с текущим временем записей ресурсов, которые имеют отметку времени, и задает текущее время на записи ресурсов, которые имеют отметку времени.  
-   Очистку записей появляется, если записи являются отметкой времени. Сервера имен (NS) записей ресурсов, в начале записей ресурсов начальной записи и записи ресурсов службы имя Windows Internet (WINS) не включаются в процесс очистки и они не являются отметкой времени, даже когда **ageallrecords**выполняется команда.  
-   Эта команда завершается ошибкой, если очистка включена для DNS-сервер и зону. Сведения о том, как включить очистку для зоны, см. в разделе **срокам** параметр синтаксисе уровня зоны в [config](#BKMK_3) команды.  
-   Добавление метки времени для записей ресурсов DNS делает их несовместимы с DNS-серверов, работающих в операционных системах, отличных от Windows 2000, Windows XP или Windows Server 2003. Метка времени, добавленные с помощью **ageallrecords** команду нельзя отменить.  
-   Если не указан ни один из необязательных параметров, то команда возвращает все записи в указанном узле. Если задано значение хотя бы для одного из необязательных параметров, **dnscmd** перечисляет только записей ресурсов, которые соответствуют значению или значения, заданные в параметрах или необязательный параметр.  
#### <a name="example"></a>Пример  
См. в разделе [пример 1: Присваивает текущее время на отметку времени для записей ресурсов](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_2"></a>dnscmd/ClearCache  
Очищает кэш-памяти DNS записей ресурсов на указанном сервере DNS.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /clearcache  
```  
#### <a name="parameters"></a>Параметры  
<ServerName>  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
#### <a name="sample-usage"></a>Пример использования  
`dnscmd dnssvr1.contoso.com /clearcache`  
### <a name="BKMK_3"></a>dnscmd/Config  
изменяет значения в реестре для отдельных зон и DNS-сервер. Принимает параметры уровня сервера и уровня зоны.  
> [!CAUTION]  
> Не изменять реестр напрямую, если нет других возможностей. Редактор реестра обходит стандартную защиту, настраивая параметры, которые могут привести к снижению производительности, повредить систему или даже потребуется переустановить Windows. Большинство параметров реестра можно безопасно изменить с помощью программ в панели управления или консоли управления (mmc). Если необходимо изменить реестр напрямую, сделайте резервную копию. Чтение Справка редактора реестра подробнее.  
#### <a name="server-level-syntax"></a>Синтаксис уровня сервера  
```  
dnscmd [<ServerName>] /config <Parameter>  
```  
#### <a name="dnscmd-config"></a>dnscmd/Config  
Изменяет конфигурацию указанного сервера.  
#### <a name="parameters"></a>Параметры  
<ServerName>  
Указывает DNS-сервер, который вы планируете управлять, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
<Parameter>  
Укажите параметр и, при необходимости значение. Значения параметров используйте следующий синтаксис: *Параметр* [*значение*]  
В оставшейся части этого раздела описываются следующие значения параметров:  
-   **/addressanswerlimit**  
-   **/bindsecondaries**  
-   **/bootmethod**  
-   **/defaultagingstate**  
-   **/defaultnorefreshinterval**  
-   **/defaultrefreshinterval**  
-   **/disableautoreversezones**  
-   **/DisableNSRecordsAutoCreation**  
-   **/dspollinginterval**  
-   **/dstombstoneinterval**  
-   **/ednscachetimeout**  
-   **/enablednsprobes**  
-   **/enablednssec**  
-   **/enableglobalnamessupport**  
-   **/enableglobalqueryblocklist**  
-   **/eventloglevel**  
-   **/forwarddelegations**  
-   **/forwardingtimeout**  
-   **/globalnamesqueryorder**  
-   **/GlobalQueryBlockList**  
-   **/isslave**  
-   **/localnetpriority**  
-   **/logfilemaxsize**  
-   **/logfilepath**  
-   **/logipfilterlist**  
-   **/LogLevel**  
-   **/MaxCacheSize**  
-   **/maxcachettl**  
-   **/namecheckflag**  
-   **/notcp**  
-   **/norecursion**  
-   **/recursionretry**  
-   **/recursiontimeout**  
-   **/RoundRobin**  
-   **/RpcProtocol**  
-   **/scavenginginterval**  
-   **/secureresponses**  
-   **/SendPort**  
-   **/strictfileparsing**  
-   **/UpdateOptions**  
-   **/writeauthorityns**  
-   **/xfrconnecttimeout**  
**/addressanswerlimit [0 | 5-28]**  
Указывает максимальное количество записей узла, DNS-сервер может отправлять в ответ на запрос. Значение может быть ноль (0), или он может быть в диапазоне от 5 до 28 записей. Значение по умолчанию равно нулю (0).  
**/bindsecondaries [0 | 1]**  
изменяет формат передачи зоны, таким образом, чтобы его можно добиться максимального сжатия и эффективность. Тем не менее этот формат несовместим с более ранними версиями из Berkeley Internet Name Domain (BIND).  
**0**  
Использование максимального сжатия. Этот формат, совместимый с версиями ПРИВЯЗКИ 4.9.4 и более поздних версий.  
**1**  
Отправляет запись только один ресурс для каждого сообщения на серверы Microsoft DNS. Этот формат совместим с более ранних версий ПРИВЯЗКИ, чем 4.9.4. Этот параметр устанавливается по умолчанию.  
**/bootmethod[0|1|2|3]**  
Определяет источник, из которого DNS-сервер получает сведения о конфигурации.  
**0**  
Удаляет источник данных конфигурации.  
**1**  
Загружает из файла ПРИВЯЗКИ, который находится в каталоге DNS, который является %systemroot%\System32\DNS по умолчанию.  
**2**  
Загружает из реестра.  
**3**  
Загружает из AD DS и реестр. Этот параметр устанавливается по умолчанию.  
**/defaultagingstate[0|1]**  
Определяет, включена ли функция очистки DNS по умолчанию на новые зоны.  
**0**  
Отключает очистки. Этот параметр устанавливается по умолчанию.  
**1**  
Включает очистки.  
**/defaultnorefreshinterval [0x1 0xFFFFFFFF | 0xA8]**  
Задает период времени, в котором принимаются без обновления, динамически обновленных записей. Это значение автоматически унаследует зон на сервере. Чтобы изменить значение по умолчанию, введите значение в диапазоне от **0x1 0xFFFFFFFF**. Значение по умолчанию с сервера — **0xA8**.  
**/defaultrefreshinterval [0x1 0xFFFFFFFF | 0xA8]**  
Задает период времени, отведенного для динамического обновления DNS-записей. Это значение автоматически унаследует зон на сервере. Чтобы изменить значение по умолчанию, введите значение в диапазоне от **0x1 0xFFFFFFFF**. Значение по умолчанию с сервера — **0xA8**.  
**/disableautoreversezones [0 | 1]**  
Включает или отключает автоматическое создание зоны обратного просмотра. Зоны обратного просмотра обеспечивают разрешение адресов протокола Интернета (IP) к доменным именам DNS.  
**0**  
Включает автоматическое создание зоны обратного просмотра. Этот параметр устанавливается по умолчанию.  
**1**  
Отключает автоматическое создание зоны обратного просмотра.  
**/disablensrecordsautocreation {0|1}**  
Указывает ли DNS-сервер автоматически создает имя сервера (NS) записи ресурсов для зоны, размещенные в нем.  
**0**  
Автоматически создает записей ресурсов сервера (NS) для зон, размещенной на сервере DNS.  
**1**  
Не создает автоматически записей ресурсов сервера (NS) для зон, размещенной на сервере DNS.  
**/dspollinginterval 0-30**  
Указывает, как часто зон, интегрированных DNS опросов сервера AD DS для изменения в active directory.  
**/dstombstoneinterval [1-30]**  
Количество времени в секундах для хранения удаленных записей в Доменных службах Active Directory.  
**/ednscachetimeout [<seconds>]**  
Указывает количество секунд кэширования расширенные сведения DNS (EDNS). Минимальное значение — 3600, а максимальное значение равно 15,724,800. Значение по умолчанию —: 604 800 секунд (одна неделя).  
**/enableednsprobes {0|1}**  
Включает или отключает сервер для проверки других серверов, чтобы определить, поддерживает ли он EDNS.  
**0**  
Отключает active поддержку EDNS пробы.  
**1**  
Включает поддержку active для EDNS проб.  
**/enablednssec {0|1}**  
Включает или отключает поддержку для расширений безопасности DNS (DNSSEC).  
**0**  
Отключает DNSSEC.  
**1**  
Разрешает DNSSEC.  
**/enableglobalnamessupport {0|1}**  
Включает или отключает поддержку зоны GlobalNames в службе. Зоны GlobalNames поддерживает разрешение однокомпонентное DNS-имен во всем лесу.  
**0**  
Отключает поддержку зоны GlobalNames в службе. При установке значения следующую команду, чтобы **0**, служба DNS-сервера не соответствует однокомпонентные имена в эту зону.  
**1**  
Включает поддержку для зоны GlobalNames в службе. При установке значения следующую команду, чтобы **1**, служба DNS разрешает однокомпонентные имена в эту зону.  
**/enableglobalqueryblocklist {0|1}**  
Включает или отключает поддержку список блокировки глобальных запросов, который блокирует разрешение имен в списке. Служба DNS-сервер создает и включает список блокировки глобальных запросов по умолчанию, при первом запуске службы. Чтобы просмотреть текущий список блокировки глобальных запросов, используйте **dnscmd/Info /globalqueryblocklist** команды.  
**0**  
Отключает поддержку список блокировки глобальных запросов. При установке значения следующую команду, чтобы **0**, служба DNS-сервер отвечает на запросы имен в списке блокировок.  
**1**  
Включает поддержку список блокировки глобальных запросов. При установке значения следующую команду, чтобы **1**, служба DNS-сервер не отвечает на запросы имен в списке блокировок.  
**/EventLogLevel [0 | 1 | 2 | 4]**  
Определяет, какие события записываются в журнал в средстве просмотра событий.  
**0**  
Регистрирует события отсутствуют.  
**1**  
Регистрируются только ошибки.  
**2**  
Записывает только ошибки и предупреждения.  
**4**  
Журналы ошибок, предупреждений и информационных событий. Этот параметр устанавливается по умолчанию.  
**/forwarddelegations [0 | 1]**  
Определяет, как DNS-сервер обрабатывает запрос для делегированного subzone. Эти запросы могут отправляться либо subzone, которая упоминается в запросе или списке серверов пересылки с именем DNS-сервера. Записи в параметре используются только в том случае, когда пересылка включена.  
**0**  
Автоматически отправляет запросы, относящиеся к делегированного подзоны для соответствующего subzone. Этот параметр устанавливается по умолчанию.  
**1**  
перенаправляет запросы, которые ссылаются на делегированного subzone для существующих серверов пересылки.  
**/forwardingtimeout [<seconds>]**  
Определяет, сколько секунд (0x1 — 0xFFFFFFFF) DNS-сервер ожидает от сервера пересылки для ответа перед попыткой другого сервера пересылки. Значение по умолчанию — **0x5**, который составляет 5 секунд.  
**/globalneamesqueryorder** {**0 | 1**}  
Указывает, ли служба сервера DNS сначала выполнит поиск в зоны GlobalNames в службе или локальной зоны при разрешении имен.  
**0**  
Служба DNS-сервер пытается разрешения имен, запросив зоны GlobalNames, прежде чем он запрашивает зоны, для которых он считается заслуживающим доверия.  
**1**  
Служба DNS-сервер пытается разрешения имен, запросив зоны, для которых он авторизован, прежде чем он запрашивает эту зону.  
**/globalqueryblocklist[[<name> [<name>]...]**  
Заменяет текущий список блокировки глобальных запросов со списком имен вами. Если вы не укажете какие-либо имена, эта команда очищает список блокировок. По умолчанию в список блокировки глобальных запросов содержит следующие элементы:  
-   ISATAP  
-   WPAD  
Служба DNS-сервера можно удалить одно или оба из следующих имен при его запуске в первый раз, при обнаружении этих имен в существующую зону.  
**/isslave [0 | 1]**  
Определяет, как сервер DNS отвечает, когда обращения, запросы, которые он перенаправляет.  
**0**  
Указывает, что DNS-сервер не является подчиненным (также известный как *подчиненного*). Если сервер пересылки не отвечает, DNS-сервер пытается разрешить сам запрос. Этот параметр устанавливается по умолчанию.  
**1**  
Указывает, что DNS-сервер является подчиненным объектом. Если сервер пересылки не отвечает, DNS-сервер завершает поиск и отправляет сообщение о сбое в сопоставителе.  
**/localnetpriority [0 | 1]**  
Определяет порядок, в котором будут возвращены записи узла, если DNS-сервер имеет несколько записей узла того же имени.  
**0**  
Возвращает записи в порядке, в котором они перечислены в базе данных DNS.  
**1**  
Возвращает записи, аналогичные IP-адрес сначала сетевые адреса. Этот параметр устанавливается по умолчанию.  
**/logfilemaxsize [<size>]**  
Указывает максимальный размер в байтах (0x10000 0xFFFFFFFF) файла Dns.log. Когда файл достигает максимального размера, DNS перезаписывает старые события. Размер по умолчанию — 0x400000, который равен 4 мегабайтам (МБ).  
**/logFilePath [< путь + Имя_файла_журнала >]**  
Указывает путь к файлу Dns.log. Путь по умолчанию — % systemroot%\System32\Dns\Dns.log. Можно указать другой путь в формате *путь + Имя_файла_журнала*.  
**/logipfilterlist <IPaddress> [,<IPaddress>...]**  
Указывает, какие пакеты регистрируются в файле журнала отладки. Операции представляют собой список всех IP-адресов. Регистрируются только те пакеты, идущие в и из IP-адреса в списке.  
**/LogLevel [<Eventtype>]**  
Определяет, какие типы событий записываются в файл Dns.log. Каждый тип события, представленного шестнадцатеричное число. Если требуется более одного события в журнале, используйте шестнадцатеричное сложения для добавьте значения, а затем введите суммы.  
**0x0**  
DNS-сервер не приводит к созданию журнала. Это запись по умолчанию.  
**0x10**  
Регистрирует запросы.  
**0x10**  
Журналы уведомлений.  
**0x20**  
Записывает в журнал обновления.  
**0xFE**  
Протоколирование nonquery транзакций.  
**0x100**  
Журналы сомневаться в транзакции.  
**0x200**  
Записывает ответы.  
**0x1000**  
Журналы отправки пакетов.  
**0x2000**  
Журналы прием пакетов.  
**0x4000**  
Регистрирует пакеты протокола UDP (User Datagram).  
**0x8000**  
Регистрирует пакеты протокола управления передачей (TCP).  
**0xFFFF**  
Регистрирует все пакеты.  
**0x10000**  
Журналы транзакций записи active directory.  
**0x20000**  
Журналы транзакций для обновления active directory.  
**0x1000000**  
Журналы полных пакетов.  
**0x80000000**  
Журналы сквозной записи транзакции.  
**/MaxCacheSize**  
Задает максимальный размер в килобайтах (КБ), кэш памяти s для DNS-сервера.  
**/maxcachettl [<seconds>]**  
Определяет, сколько секунд (0x0 — 0xFFFFFFFF) запись сохраняется в кэше. Если **0x0** параметр используется, DNS-сервер не кэширует записи. Значение по умолчанию — **0x15180** (86 400 секундам или 1 день).  
**/MaxNegativeCacheTtl [<seconds>]**  
Указывает, сколько секунд (0x1 — 0xFFFFFFFF) запись, которая записывает отрицательный ответ на запрос будет сохранен в кэше DNS. Значение по умолчанию — **0x384** (900 секунд).  
**/namecheckflag [0 | 1 | 2 | 3].**  
Указывает, какой символ стандарт используется при проверке DNS-имена.  
**0**  
Использует символы ANSI, которые соответствуют задаче разработки Интернет принудительно (IETF) RFC-запрос.  
**1**  
Использует символы ANSI, которые не обязательно соответствуют с документах RFC от IETF.  
**2**  
Многобайтовый UCS преобразование использует формат 8 символов (UTF-8). Этот параметр устанавливается по умолчанию.  
**3**  
Использует все символы.  
**/norecursion [0 | 1]**  
Определяет, выполняет ли DNS-сервер рекурсивного разрешения имен.  
**0**  
DNS-сервер выполняет рекурсивного разрешения имен, если она запрошена в запросе. Этот параметр устанавливается по умолчанию.  
**1**  
DNS-сервер не выполняет рекурсивного разрешения имен.  
**/notcp**  
Этот параметр является устаревшим, и он не оказывает влияния в текущих версиях Windows Server.  
**/recursionretry [<seconds>]**  
Определяет количество секунд (0x1 — 0xFFFFFFFF), DNS-сервер ожидает, прежде чем снова пытаться связаться с сервером удаленного. Значение по умолчанию — 0x3 (три секунды). Это значение должно быть увеличено, при возникновении рекурсии по каналу медленной глобальной сети (WAN).  
**/recursiontimeout [<seconds>]**  
Определяет количество секунд (0x1 — 0xFFFFFFFF), DNS-сервер ожидает до прекращения попытки связаться с удаленного сервера. От режима **0x1** через **0xFFFFFFFF**. Значение по умолчанию — **0xF** (15 секунд). Это значение должно быть увеличено, при возникновении рекурсии по медленной глобальной сети.  
**/RoundRobin [0 | 1]**  
Определяет порядок, в котором будут возвращены записи узла, если сервер имеет несколько записей узла того же имени.  
0  
DNS-сервер не использует циклический перебор. Вместо этого он возвращает первую запись в каждый запрос.  
**1**  
DNS-сервер чередование записей, которые он возвращает сверху вниз список совпадающих записей. Этот параметр устанавливается по умолчанию.  
**/rpcprotocol [0x0|0x1|0x2|0x4|0xFFFFFFFF]**  
Указывает протокол, который использует удаленный вызов процедур (RPC), он создает подключение от DNS-сервера.  
**0x0**  
Отключает RPC для DNS.  
**0x1**  
Использует протокол TCP/IP.  
**0x2**  
Использует именованные каналы.  
**0x4**  
Использует вызов локальной процедуры (LPC).  
**0xFFFFFFFF**  
Все протоколы. Этот параметр устанавливается по умолчанию.  
**/scavenginginterval [<hours>]**  
Определяет, включена ли функция очистки для DNS-сервера и задает количество часов (0x0 — 0xFFFFFFFF) между очистки циклов. Значение по умолчанию — **0x0**, отключающее очистки для DNS-сервера. Параметр больше, чем **0x0** обеспечивает очистки для сервера и устанавливает число часов между циклами очистки.  
**/secureresponses [0 | 1]**  
Определяет, фильтрует ли DNS записей, которые сохраняются в кэше.  
**0**  
Сохраняет все ответы на запросы имен к кэшу. Этот параметр устанавливается по умолчанию.  
**1**  
Сохраняет только записи, которые принадлежат к данном поддереве DNS в кэш.  
**/sendport [<port>]**  
Указывает номер порта (0x0 — 0xFFFFFFFF), который DNS используется для отправки рекурсивные запросы к другим серверам DNS. Значение по умолчанию — **0x0**, что означает, что номер порта выбирается случайным образом.  
**/serverlevelplugindll[<Dllpath>]**  
Указывает путь к пользовательский подключаемый модуль. Когда *Dllpath* полный путь и имя допустимым подключаемого модуля, DNS-сервер вызывает функции в подключаемом модуле для разрешения запросов имен, выходящие за рамки все локально размещенных зон DNS-сервера. Если запрошенное имя выходит за рамки подключаемый модуль, DNS-сервер выполняет разрешение имен с помощью переадресации или рекурсию, согласно настройкам. Если *Dllpath* не указан, DNS-сервер перестает использовать пользовательский подключаемый модуль, если пользовательский подключаемый модуль уже был настроен.  
**/strictfileparsing [0 | 1]**  
Определяет поведение DNS сервера, когда он встречает ошибочные записи при загрузке зоны.  
**0**  
DNS-сервер по-прежнему загрузить зону, даже если сервер обнаруживает ошибочные записи. Эта ошибка записана в журнал DNS. Этот параметр устанавливается по умолчанию.  
**1**  
DNS-сервер останавливает загрузку зоны, а также регистрирует ошибки в журнал DNS.  
**/UpdateOptions <RecordValue>**  
Не разрешены динамические обновления определенных типов записей. Если требуется более чем один тип записей, которые запрещено в журнале, используйте шестнадцатеричное сложения для добавьте значения, а затем введите суммы.  
**0x0**  
Не ограничивает любые типы записей.  
**0x1**  
Исключает начала записей ресурсов начальной записи.  
**0x2**  
Исключает записей ресурсов сервера (NS).  
**0x4**  
Исключает делегирование записей ресурсов сервера (NS).  
**0x8**  
Исключает записи сервера.  
**0x100**  
Во время безопасного динамического обновления исключает начала записей ресурсов начальной записи.  
**0x200**  
Во время безопасного динамического обновления исключает корневой записей ресурсов сервера (NS).  
**0x30F**  
Во время стандартных динамического обновления исключает записей ресурсов сервера (NS), записей ресурсов начальной записи, и в записи сервера. Во время безопасного динамического обновления исключает записей ресурсов сервера (NS) корневой и начале записей ресурсов начальной записи. Позволяет делегирования и серверу обновления сервера.  
**0x400**  
Во время безопасного динамического обновления исключает делегирования записей ресурсов сервера (NS).  
**0x800**  
Во время безопасного динамического обновления исключает записи сервера.  
**0x1000000**  
Исключает записи делегирования делегации (DS).  
**0x80000000**  
Отключение динамического обновления DNS.  
**/writeauthorityns [0 | 1]**  
Определяет, когда DNS-сервер записывает записей ресурсов сервера (NS) в разделе Центр ответа.  
**0**  
Записывает записей ресурсов сервера (NS) в разделе Центр только ссылки. Этот параметр соответствует с Rfc 1034, средства и основные понятия имена домена и Rfc 2181, уточнения спецификации DNS. Этот параметр устанавливается по умолчанию.  
**1**  
Записывает записей ресурсов сервера (NS) в разделе Центр всех успешных ответов заслуживающих доверия.  
**/xfrconnecttimeout [<seconds>]**  
Определяет количество секунд (0x0 — 0xFFFFFFFF), основной DNS-сервер ожидает передачи ответа от сервера-получателя. Значение по умолчанию — **0x1E** (30 секунд). После истечения срока действия значение времени ожидания, соединение будет разорвано.  
#### <a name="zone-level-syntax"></a>Синтаксис уровня зоны  
```  
dnscmd /config <Parameters>  
```  
#### <a name="dnscmd-config"></a>dnscmd/Config  
Изменяет конфигурацию указанной зоны.  
#### <a name="parameters"></a>Параметры  
**<Parameters>**  
Задайте параметр, зона, имя и, в качестве альтернативы значение. Значения параметров используйте следующий синтаксис: *Параметр ZoneName* [*значение*]  
В оставшейся части этого раздела описаны следующие значения параметров:  
-   **/ устаревания**  
-   **/AllowNSRecordsAutoCreation**  
-   **/allowupdate**  
-   **/forwarderslave**  
-   **/forwardertimeout**  
-   **/norefreshinterval**  
-   **/refreshinterval**  
-   **/securesecondaries**  
**/ устаревания <ZoneName>**  
Включает или отключает очистки в определенной зоне.  
**/AllowNSRecordsAutoCreation <ZoneName> [<Value>]**  
Переопределяет DNS сервера имя сервера (NS) ресурсов записей автоматическое создание параметра. Имя сервера (NS) записи, которые были ранее зарегистрированы для этой зоны, не затрагиваются. Таким образом необходимо удалить их вручную, если вы не хотите их.  
**/allowupdate <ZoneName>**  
Определяет, принимает ли указанной зоны динамические обновления.  
**/forwarderslave <ZoneName>**  
Переопределяет DNS-сервер **/isslave** параметр.  
**/forwardertimeout <ZoneName>**  
Определяет, сколько секунд зону DNS ожидает от сервера пересылки для ответа перед попыткой другого сервера пересылки. Это значение переопределяет значение, которое задается на уровне сервера.  
**/norefreshinterval <ZoneName>**  
Задает интервал времени для зоны, во время которых нет обновлений можно динамически обновлять записи DNS в указанной зоне.  
**/refreshinterval <ZoneName>**  
Задает интервал времени для зоны, в течение которого обновления можно динамически обновлять записи DNS в указанной зоне.  
**/securesecondaries <ZoneName>**  
Определяет, какие серверы-получатели могут получать обновления зоны с главного сервера для этой зоны.  
#### <a name="remarks"></a>Примечания  
-   Имя зоны необходимо указать только для параметров на уровне зоны.  
### <a name="BKMK_4"></a>dnscmd /createbuiltindirectorypartitions  
Создает раздел каталога приложений DNS. При установке DNS раздела каталога приложений для службы создается на уровне леса и домена. Эта команда используется для создания разделов каталога приложений DNS, которые были удалены или еще не создан. Без параметров эта команда создает встроенные раздела каталога DNS для домена.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /createbuiltindirectorypartitions [/forest] [/alldomains]   
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**/ Forest**  
Создает раздел каталога DNS для леса.  
**/ AllDomains**  
создает разделы DNS для всех доменов в лесу.  
### <a name="BKMK_5"></a>dnscmd /createdirectorypartition  
Создает раздел каталога приложений DNS. При установке DNS раздела каталога приложений для службы создается на уровне леса и домена. Эта операция создает дополнительные разделы каталога приложений DNS.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /createdirectorypartition <PartitionFQDN>  
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<PartitionFQDN>**  
Полное ДОМЕННОЕ имя раздела каталога приложений DNS, который будет создан.  
### <a name="BKMK_6"></a>dnscmd /deletedirectorypartition  
Удаляет существующий раздел каталога приложений DNS.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /deletedirectorypartition <PartitionFQDN>  
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<PartitionFQDN>**  
Полное ДОМЕННОЕ имя раздела каталога приложений DNS, которая будет удалена.  
### <a name="BKMK_7"></a>dnscmd /directorypartitioninfo  
Выводит сведения о указанного раздела каталога приложений DNS.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /directorypartitioninfo <PartitionFQDN> [/detail]   
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<PartitionFQDN>**  
Полное ДОМЕННОЕ имя раздела каталога приложений DNS.  
**/ Detail**  
Выводит все сведения о разделе каталога приложений.  
### <a name="BKMK_8"></a>dnscmd /enlistdirectorypartition  
Добавляет DNS-сервера к указанному разделу каталога в наборе реплик.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /enlistdirectorypartition <PartitionFQDN>  
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<PartitionFQDN>**  
Полное ДОМЕННОЕ имя раздела каталога приложений DNS.  
### <a name="BKMK_9"></a>dnscmd /enumdirectorypartitions  
Список разделов каталога приложений DNS для указанного сервера.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /enumdirectorypartitions [/custom]   
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**/ пользовательский**  
перечислены только разделов каталога, созданных пользователем.  
### <a name="BKMK_10"></a>dnscmd /enumrecords  
Возвращает список записей ресурсов указанного узла в зоне DNS.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /enumrecords <ZoneName> <NodeName> [/type <RRtype> <Rrdata>] [/authority] [/glue] [/additional] [/node | /child | /startchild<ChildName>] [/continue | /detail]   
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер, который вы планируете управлять, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**/enumrecords**  
список записей ресурсов в указанной зоне.  
**<ZoneName>**  
Указывает имя зоны, к которой принадлежат записей ресурсов.  
**<NodeName>**  
Указывает имя узла записей ресурсов.  
**или типа <RRtype> <Rrdata>**  
Указывает тип записей ресурсов, которые будут указаны и тип данных, которая ожидается:  
**<RRtype>**  
Указывает тип записей ресурсов, которые будут указаны.  
**<Rrdata>**  
Указывает тип данных, которая содержит ожидаемые записи.  
**/Authority**  
Включает в себя достоверные данные.  
**/Glue**  
Включает связующий данные.  
**/ Дополнительные**  
Включает все дополнительные сведения о записях ресурса.  
**{/node | /child | /startchild <ChildName>}**  
Фильтрует или добавляет информацию для отображения записей ресурсов:  
**/ node**  
перечислены только записей ресурсов указанного узла.  
**/child**  
перечислены только записей ресурсов указанного дочернего домена.  
**/StartChild <ChildName>**  
Начинает указанный дочерний домен в списке.  
**/ continue | / Detail**  
Указывает способ отображения возвращаемых данных.  
**/ continue**  
перечислены только записей ресурсов с их типом и данных.  
**/ Detail**  
Выводит все сведения о записях ресурсов.  
#### <a name="sample-usage"></a>Пример использования  
`dnscmd /enumrecords test.contoso.com test /additional`  
### <a name="BKMK_11"></a>dnscmd /enumzones  
Список зон, которые существуют на указанном сервере DNS.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /enumzones [/primary | /secondary | /forwarder | /stub | /cache | /auto-created] [/forward | /reverse | /ds | /file] [/domaindirectorypartition | /forestdirectorypartition | /customdirectorypartition | /legacydirectorypartition | /directorypartition <PartitionFQDN>]  
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**/primary | /secondary | /forwarder | /stub | /cache | /auto-created**  
Фильтры типов зон для отображения:  
**/ primary**  
список всех зон, которые стандартных основных зонах или интегрированные зоны active directory.  
**/ -получателя**  
перечислены все стандартные дополнительные зоны.  
**/forwarder**  
Список зон, которые пересылают неразрешенных запросов на другой сервер DNS.  
**/ STUB**  
список всех зон заглушки.  
**/ Cache**  
перечислены только зоны, которые загружаются в кэш.  
**/ автоматически созданная**  
Список зон, которые были автоматически созданы во время установки сервера DNS.  
**/forward | /reverse | /ds | /file**  
Определяет дополнительные фильтры типа из зон для отображения:  
**/ вперед**  
списки зоны прямого просмотра.  
**/ обратный**  
списки зоны обратного просмотра.  
**/DS**  
Список зон, интегрированных в active directory.  
**/ File**  
Список зон, связанных с файлами.  
**/domaindirectorypartition**  
Список зон, которые хранятся в разделе каталога домена.  
**/forestdirectorypartition**  
Список зон, которые хранятся в лесу раздела каталога приложений DNS.  
**/customdirectorypartition**  
список всех зон, которые хранятся в раздел каталога приложений, определяемые пользователем.  
**/legacydirectorypartition**  
список всех зон, которые хранятся в разделе каталога домена.  
**/directorypartition <PartitionFQDN>**  
список всех зон, которые хранятся в указанном разделе каталога.  
#### <a name="remarks"></a>Примечания  
-   **Enumzones** параметры действуют как фильтры для списка зон. Если не указано ни одного фильтра, возвращается полный список зон. Если указан фильтр, только зоны, которые соответствуют критериям этот фильтр включаются в возвращаемый список зон.  
#### <a name="example"></a>Пример  
См. в разделе [пример 2: Отобразить полный список зон DNS-сервера](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx) или [пример 3: Отобразить список автоматически зоны на DNS-сервере](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_25a"></a>dnscmd /exportsettings  
создает текстовый файл, содержащий сведения о конфигурации DNS-сервера. Текстовый файл называется DnsSettings.txt. Он находится в каталоге %systemroot%\system32\dns сервера.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /exportsettings   
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
#### <a name="remarks"></a>Примечания  
-   Можно использовать информацию в файле, **dnscmd /exportsettings** создает для устранения проблем с конфигурацией или убедитесь, что вы настроили несколько серверов одинаково.  
### <a name="BKMK_12"></a>dnscmd/Info  
Отображает параметры из раздела реестра указанного сервера DNS: **HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\DNS\Parameters**  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /info [<Setting>]  
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<Setting>**  
Установка, **info** возвращает команда может быть установлен индивидуально. Если параметр не указан, возвращается отчет об общих параметрах.  
#### <a name="remarks"></a>Примечания  
-   Эта команда отображает параметры реестра, которые находятся на уровне сервера DNS. Чтобы отобразить параметры реестра уровня зоны, используйте [zoneinfo](#BKMK_26) команды. Чтобы просмотреть список параметров, которые могут отображаться с помощью следующей команды, см. в разделе [config](#BKMK_3) описание.  
#### <a name="example"></a>Пример  
См. в разделе [пример 4: Отображает значение IsSlave от DNS-сервера](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx) или [пример 5: Отображает значение Recursiontimeout от DNS-сервера](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_29a"></a>dnscmd /ipvalidate  
Проверяет, является ли IP-адрес идентифицирует работающим DNS-сервером или ли DNS-сервер может выступать в качестве сервера пересылки, сервером корневых или главный сервер для конкретной зоны.  
#### <a name="syntax"></a>Синтаксис  
```  
dnscmd [<ServerName>] /ipvalidate <Context> [<ZoneName>] [[<IPaddress>]]  
```  
#### <a name="parameters"></a>Параметры  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<Context>**  
Указывает тип теста для выполнения. Можно указать любой из следующих тестов:  
-   **/dnsservers** проверяет работоспособность DNS-серверы для компьютеров с вами адреса.  
-   **/forwarders** проверяет, что указанные адреса определить DNS-серверы, которые могут выступать в качестве серверов пересылки.  
-   **/roothints** проверяет, что указанные адреса определить DNS-серверы, которые могут выступать в качестве корневых серверов доменных имен подсказку.  
-   **/zonemasters** проверяет, что указанные адреса определить DNS-серверы, которые являются главных серверов о *ZoneName*.  
**<ZoneName>**  
Определяет зону. Используйте этот параметр с **/zonemasters** параметра.  
**<IPaddress>**  
Указывает IP-адреса, которые команда проверяет.  
#### <a name="sample-usage"></a>Пример использования  
<pre>dnscmd dnssvr1.contoso.com /ipvalidate /dnsservers 10.0.0.1 10.0.0.2  
dnscmd dnssvr1.contoso.com /ipvalidate /zonemasters corp.contoso.com 10.0.0.2</pre>  
### <a name="BKMK_13"></a>dnscmd /nodedelete  
Удаляет все записи для указанного узла. ### Синтаксис ```  
dnscmd [<ServerName>] /nodedelete <ZoneName> <NodeName> [/tree] [/ f] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает имя зоны.  
**<NodeName>**  
Указывает имя узла, узла для удаления.  
**/Tree**  
Удаляет все дочерние записи.  
**/f**  
Выполняет команду без запроса на подтверждение. ### См. пример [пример 6: удаление записей из узла](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_14"></a>dnscmd/RecordAdd  
Добавляет запись для указанной зоны в DNS-сервер. ### Синтаксис ```  
dnscmd [<ServerName>] / RecordAdd <ZoneName> <NodeName> <RRtype> <Rrdata> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает зону, в которой находится запись.  
**<NodeName>**  
Указывает конкретный узел в зону.  
**<RRtype>**  
Указывает тип записи для добавления.  
**<Rrdata>**  
Указывает тип данных, которая ожидается.  
> [!NOTE]  
При добавлении записи убедитесь, что используется формат типа и данные правильные данные. Список типов записей ресурсов и соответствующие типы данных, см. в разделе [ссылку записей ресурсов](https://technet.microsoft.com/library/cc758321(v=ws.10).aspx). ### Пример использования
<pre>dnscmd dnssvr1.contoso.com /recordadd test A 10.0.0.5  
dnscmd /recordadd test.contoso.com test MX 10 mailserver.test.contoso.com</pre>  
### <a name="BKMK_15"></a>dnscmd /recorddelete  
Удаляет запись ресурса из указанной зоны. ### Синтаксис ```  
dnscmd <ServerName> /recorddelete <ZoneName> <NodeName> <RRtype> <Rrdata>[/ f] ```  
#### Parameters  
**<ServerName>**  
> Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает зону, в которой находится записи ресурса.  
**<NodeName>**  
Задает имя узла.  
**<RRtype>**  
Указывает тип записи ресурса для удаления.  
**<Rrdata>**  
Указывает тип данных, которая ожидается.  
**/f**  
Выполняет команду без запроса на подтверждение:-, так как узлы могут иметь более одной записи ресурсов, эту команду необходимо обладать очень указывать тип записи ресурса, который требуется удалить. -Если указать тип данных и тип данные записи ресурсов не указан, удаляются все записи с этим типом данных для указанного узла. ### Пример использования `dnscmd /recorddelete test.contoso.com test MX 10 mailserver.test.contoso.com`  
### <a name="BKMK_16"></a>dnscmd /resetforwarders  
выбирает или сбрасывает IP-адреса, к которым DNS-сервер перенаправляет запросы DNS, если его не удается разрешить их локально. ### Синтаксис ```  
dnscmd [<ServerName>] /resetforwarders [<IPaddress> [,<IPaddress>]...] [/ timeout <timeOut>] [-ведомый | / noslave] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<IPaddress>**  
Список IP-адресов, к которым DNS-сервер перенаправляет запросы неразрешенных.  
** / timeout <timeOut>**  
Задает количество секунд ожидания ответа от сервера пересылки DNS-сервера. По умолчанию это значение составляет пять секунд.  
**/slave|/noslave**  
Определяет, выполняет ли собственный итеративные запросы DNS-сервер при сбое сервера пересылки для разрешения запросов:  
**/ slave**  
Запрещает выполнять собственную итеративные запросы, при сбое сервера пересылки для разрешения запросов DNS-сервера.  
**/ NoSlave**  
Позволяет серверу выполнять собственную итеративные запросы при сбое сервера пересылки для разрешения запросов DNS. Этот параметр устанавливается по умолчанию. ### Примечания — по умолчанию, DNS-сервер выполняет итеративные запросы при невозможности разрешить запрос. — Задание IP-адресов с помощью **resetforwarders** команда приводит к DNS-сервер для выполнения рекурсивных запросов на DNS-серверы в указанных IP-адресов. Если запрос не решат серверы пересылки, DNS-сервера можно выполнить собственную итеративные запросы. -Если **— ведомый** параметр используется, DNS-сервер не выполняет свои собственные итеративные запросы. Это означает, что DNS-сервер перенаправляет неразрешенные запросы только DNS-серверы в списке, и он не пытайтесь итеративные запросы, если серверы пересылки, не помогли устранить их. Он является более эффективным, задать один IP-адрес сервера пересылки для DNS-сервера. Можно использовать **resetforwarders** команду для внутренних серверов в сети, чтобы пересылать их неразрешенные запросы один DNS-сервер, имеющий подключение к внешнему. — Список IP-адрес сервера пересылки дважды приводит к DNS-сервера, попытка дважды вперед к этому серверу. ### Пример использования
<pre>dnscmd dnssvr1.contoso.com /resetforwarders 10.0.0.1 /timeout 7 /slave  
dnscmd dnssvr1.contoso.com /resetforwarders /noslave</pre>  
### <a name="BKMK_17"></a>dnscmd /resetlistenaddresses  
Указывает IP-адреса на сервере, который прослушивает запросы клиента DNS. ### Синтаксис ```  
dnscmd [<ServerName>] /resetlistenaddresses [<listenaddress>] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<listenaddress>**  
Указывает IP-адрес на DNS-сервере, который прослушивает запросы клиента DNS. Если адрес прослушивания не указан, все IP-адреса на сервере будет прослушивать запросы клиента. ### Примечания — по умолчанию все IP-адреса DNS-сервера прослушивание клиентских запросов DNS. ### Пример использования `dnscmd dnssvr1.contoso.com /resetlistenaddresses 10.0.0.1`  
### <a name="BKMK_18"></a>dnscmd /startscavenging  
Указывает DNS-сервер, попытка немедленного поиск устаревших записей ресурсов в указанный DNS-сервер. ### Синтаксис ```  
dnscmd [<ServerName>] /startscavenging ```  
#### Parameter  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер. ### Примечания — успешное завершение этой команды немедленно начинает удалять. -Несмотря на то, что для успешного завершения отображается команда запуска удалять, удалять не запускается, пока не будут выполнены следующие предварительные условия:-очистки включен для сервера и зоны. -Запускается зоне. -Записи ресурса имеют отметку времени. — сведения о включении очистки для сервера, см. в разделе **scavenginginterval** параметр синтаксисе уровня сервера в [config](#BKMK_3) раздел. — сведения о том, как включить очистку для зоны, см. в разделе **время существования** параметр синтаксисе уровня зоны в [config](#BKMK_3) раздел. — сведения о запуске приостановленной зоны, см. в разделе [zoneresume](#BKMK_35) раздел. — сведения о том, как проверить записи ресурсов для отметки времени, см. в разделе [ageallrecords](#BKMK_1) раздел. -Если удалять завершается ошибкой, не появится предупреждение. ### Пример использования `dnscmd dnssvr1.contoso.com /startscavenging`  
### <a name="BKMK_19"></a>dnscmd/STATISTICS  
Отображает или удаляет данные для указанного DNS-сервера. ### Синтаксис ```  
dnscmd [<ServerName>] / STATISTICS [<StatID>] [/ clear] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<StatID>**  
Указывает, какой статистики или сочетание статистику для отображения. Идентификационный номер используется для идентификации статистического показателя. Если номер идентификатор статистики не задан, отображаются все статистические данные.  
Ниже приведен список чисел, который может быть указан и соответствующий статистического показателя, который отображает:  
**00000001**  
time  
**00000002**  
запрос  
**00000004**  
query2  
**00000008**  
Recurse  
**00000010**  
Главная  
**00000020**  
Сервер  
**00000040**  
WINS  
**00000100**  
Обновление  
**00000200**  
SkwanSec  
**00000400**  
доменных служб Active Directory  
**00010000**  
Память  
**00100000**  
PacketMem  
**00040000**  
Для dBASE  
**00080000**  
Записи  
**00200000**  
NbstatMem  
**/ clear**  
указанной статистики счетчик сбрасывается до нуля. ### Примечания — **статистики** команда отображает счетчики, которые начинаются на DNS-сервере, при запуске или возобновлении. ### Примеры см. в разделе [пример 7: Отображение статистики о времени для DNS-сервера](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx) или [примере 8: Отображение статистики NbstatMem для DNS-сервера](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_20"></a>dnscmd /unenlistdirectorypartition  
Удаляет DNS-сервера из набора реплик секции указанного каталога. ### Синтаксис ```  
dnscmd [<ServerName>] /unenlistdirectorypartition <PartitionFQDN> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<PartitionFQDN>**  
Полное ДОМЕННОЕ имя раздела каталога приложений DNS, которая будет удалена.  
### <a name="BKMK_21"></a>dnscmd /writebackfiles  
Проверяет память сервера DNS для изменения и записывает их в постоянное хранилище. ### Синтаксис ```  
dnscmd [<ServerName>] /writebackfiles [<ZoneName>] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, чтобы обновить. ### Примечания — **writebackfiles** команда обновляет все "грязные" зоны или указанной зоны. Зона является "грязные", при наличии изменений в памяти, которые еще не были записаны в постоянное хранилище. Это операция на уровне сервера, который проверяет все зоны. В этой операции можно указать одну зону или воспользоваться [zonewriteback](#BKMK_37) операции. ### Пример использования `dnscmd dnssvr1.contoso.com /writebackfiles`  
### <a name="BKMK_22"></a>dnscmd /zoneadd  
Добавляет зону на DNS-сервер. ### Синтаксис ```  
dnscmd [<ServerName>] /zoneadd <ZoneName> <Zonetype> [/dp <FQDN>| {/ домена | / enterprise | / устаревших}] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает имя зоны.  
**<Zonetype>**  
Указывает тип зоны, для создания. Каждый тип зоны имеет различные необходимые параметры:  
**/ DsPrimary**  
Создает зоны, интегрированной в active directory.  
** / первичного/File <FileName>**  
Создает стандартной основной зоны и указывает имя файла, в которой будут храниться данные зоны.  
** или вторичных <MasterIPaddress> [<MasterIPaddress>...] **  
Создает стандартный дополнительной зоны.  
** / stub <MasterIPaddress> [<MasterIPaddress>...] / file <FileName>**  
Создает зона-заглушка с файловой поддержкой.  
** / dsstub <MasterIPaddress> [<MasterIPaddress>...] **  
Создает зоны заглушки интегрированной с active directory.  
** / сервер пересылки <MasterIPaddress> [<MasterIPaddress>]... / file <FileName>**  
Указывает, что созданный зоны перенаправляет неразрешенные запросы на другой сервер DNS.  
**/dsforwarder**  
Указывает, что созданный интегрированной зоне active directory перенаправляет неразрешенные запросы на другой сервер DNS.  
**/dp <FQDN> {/domain | /enterprise | /legacy}**  
Задает раздел каталога, в которой хранятся зоны.  
**<FQDN>**  
Указывает полное ДОМЕННОЕ имя раздела каталога.  
**/ domain**  
Сохранение зоны на раздел каталога домена.  
**/Enterprise**  
Сохранение зоны на раздел каталога enterprise.  
**/ прежних версий**  
Сохранение зоны на устаревшего раздела каталога. ### Примечания —, указав тип **/forwarder** или **/dsforwarder** создается зона, которая выполняет условное перенаправление. ### Пример использования
<pre>dnscmd dnssvr1.contoso.com /zoneadd test.contoso.com /dsprimary  
dnscmd dnssvr1.contoso.com /zoneadd secondtest.contoso.com /secondary 10.0.0.2</pre>  
### <a name="BKMK_23"></a>dnscmd /zonechangedirectorypartition  
изменяет данный раздел каталога, на котором находится указанной зоны. ### Синтаксис ```  
dnscmd [<ServerName>] /zonechangedirectorypartition <ZoneName>] {[<NewPartitionName>] | [<Zonetype>] } ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Полное ДОМЕННОЕ имя текущей секции каталога, на котором находится зона.  
**<NewPartitionName>**  
Полное ДОМЕННОЕ имя раздела каталога, который будут перемещены зоны.  
**<Zonetype>**  
Задает тип раздела каталога, в который будут перемещены зоны.  
**/ domain**  
Перемещает зоны встроенные разделе каталога.  
**/ Forest**  
Перемещает зоны к разделу каталога встроенные леса.  
**/ прежних версий**  
Перемещает зоны к разделу каталога, созданного для контроллеров домена active directory предварительно. Эти разделы каталога не обязательны для основного режима.  
### <a name="BKMK_24"></a>dnscmd /zonedelete  
Удаление указанной зоны. ### Синтаксис ```  
dnscmd [<ServerName>] /zonedelete <ZoneName> [/dsdel] [/ f] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, для удаления.  
**/dsdel**  
Удаление зоны из AD DS.  
**/f**  
Выполняет команду без запроса на подтверждение. ### Пример см. в разделе [9 пример: удаление зоны от DNS-сервера](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_25"></a>dnscmd /zoneexport  
создает текстовый файл со списком записей ресурсов из указанной зоны. ### Синтаксис "dnscmd [<ServerName>] /zoneexport <ZoneName> <ZoneExportFile>" ### Параметры **<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает имя зоны.  
**<ZoneExportFile>**  
Указывает имя создаваемого файла. ### Примечания — **zoneexport** операция создает файл записей ресурсов для интегрированной зоне active directory для устранения неполадок. По умолчанию, эта команда создает файл помещается в каталог DNS, который используется по умолчанию каталог %systemroot%/System32/Dns. ### См. пример [пример 10: Экспортировать список записей ресурсов для зоны в файл](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_26"></a>dnscmd /zoneinfo  
Отображает параметры из раздела реестра из указанной зоны: ** HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\DNS\Parameters\Zones\\<ZoneName>** ### синтаксис ```  
dnscmd [<ServerName>] /zoneinfo <ZoneName> [<Setting>] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает имя зоны.  
**<Setting>**  
По отдельности можно указать любой параметр, который **zoneinfo** командой. Если параметр не указан, возвращаются все параметры. ### Примечания — **zoneinfo** команда отображает параметры реестра, которые находятся на зоны DNS на уровне ** HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\DNS\Parameters\Zones\\<ZoneName>**. -Для отображения параметров реестра на уровне сервера используйте [info](#BKMK_12) команды. -Для просмотра списка параметров, которые можно отобразить с помощью следующей команды, см. в разделе [config](#BKMK_3) команды. ### См. пример [пример 11: Отображает значение RefreshInterval из реестра](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx) или [пример 12: Отображаемое время существования параметр из реестра](https://technet.microsoft.com/library/cc784399(v=ws.10).aspx).  
### <a name="BKMK_27"></a>dnscmd /zonepause  
Приостанавливает указанной зоны, затем не обрабатывает запросы. ### Синтаксис ```  
dnscmd [<ServerName>] /zonepause <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, которую необходимо приостановить. ### Примечания — возобновить зону и сделать его доступным после приостановки, [zoneresume](#BKMK_35) команды. ### Пример использования `dnscmd dnssvr1.contoso.com /zonepause test.contoso.com`  
### <a name="BKMK_28"></a>dnscmd /zoneprint  
Возвращает список записей в зоне. ### Синтаксис ```  
dnscmd [<ServerName>] /zoneprint <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Определяет зону, в которых будет указано.  
### <a name="BKMK_30"></a>dnscmd /zonerefresh  
заставляет дополнительную зону DNS для обновления из основной зоны. ### Синтаксис ```  
dnscmd <ServerName> /zonerefresh <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Задает имя зоны, которую требуется обновить. ### Примечания — **zonerefresh** команда принудительно выполняет проверку номер версии в начало главного сервера s записи (SOA). Если номер версии на главном сервере больше, чем номер версии сервера-получателя, инициируется передачу зоны, который обновляет на сервере-получателе. Если номер версии совпадают, происходит без передачи зоны. — По умолчанию Принудительная проверка происходит каждые 15 минут. Чтобы изменить значение по умолчанию, используйте **dnscmd config refreshinterval** команды. ### Пример использования `dnscmd dnssvr1.contoso.com /zonerefresh test.contoso.com`  
### <a name="BKMK_31"></a>dnscmd /zonereload  
Копии зоны сведения из источника. ### Синтаксис ```  
dnscmd <ServerName> /zonereload <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны в перезагрузке. ### Примечания — Если зона является встроенная проверка подлинности active directory, он перезагружает из AD DS. -Если зона является стандартной файловой поддержкой зоны, он перезагружает из файла. ### Пример использования `dnscmd dnssvr1.contoso.com /zonereload test.contoso.com`  
### <a name="BKMK_32"></a>dnscmd /zoneresetmasters  
Сбрасывает IP-адреса главного сервера, предоставляющий сведения о передачи зоны для дополнительной зоны. ### Синтаксис ```  
dnscmd <ServerName> /zoneresetmasters <ZoneName> [/ локального] [<IPaddress> [<IPaddress>]...] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны в перезагрузке.  
**/ локального**  
Задает локальный главный список. Этот параметр используется для зон, интегрированных в active directory.  
**<IPaddress>**  
IP-адреса главных серверов дополнительной зоны. ### Примечания — это значение изначально задано значение, когда создается дополнительная зона. Используйте **zoneresetmasters** команду на сервере-получателе. Если установлено на главном сервере DNS, это значение игнорируется. ### Пример использования
<pre>dnscmd dnssvr1.contoso.com /zoneresetmasters test.contoso.com 10.0.0.1  
dnscmd dnssvr1.contoso.com /zoneresetmasters test.contoso.com /local</pre>  
### <a name="BKMK_33"></a>dnscmd /zoneresetscavengeservers  
Изменение IP-адреса серверов, которые можно очистить указанной зоны. ### Синтаксис ```  
dnscmd [<ServerName>] /zoneresetscavengeservers <ZoneName> [<IPaddress> [<IPaddress>]...] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Идентифицирует зону для очистки.  
**<IPaddress>**  
Список IP-адресов серверов, которые могут выполнять удалять. Если этот параметр указан, все серверы, на которых размещены эту зону можно очистить его. ### Remarks - по умолчанию все серверы, на которых размещаются зоны можно очистить этой зоны. -Если зоны размещается на нескольких DNS-сервер, эта команда используется для уменьшения количества раз в сутки зоны. -Очистки необходимо включить на DNS-сервер и зону, затронутых этой командой. ### Пример использования `dnscmd dnssvr1.contoso.com /zoneresetscavengeservers test.contoso.com 10.0.0.1 10.0.0.2`  
### <a name="BKMK_34"></a>dnscmd /zoneresetsecondaries  
Указывает список IP-адреса дополнительных серверов, на которые главный сервер отвечает, ответ на запрос для передачи зоны. ### Синтаксис ```  
dnscmd [<ServerName>] /zoneresetsecondaries <ZoneName> {/ noxfr | / небезопасные | /securens | / SecureList <SecurityIPaddresses>} {/ nonotify | / уведомлять | /notifylist <NotifyIPaddresses>} ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, будет иметь его серверы-получатели сброса.  
**/noxfr | /nonsecure | /securens | /securelist <SecurityIPaddresses>**  
Задает ли все или только некоторые из серверов-получателей, запрос обновления получения обновления.  
**/noxfr**  
Указывает, что разрешены без передачи зоны.  
**/ небезопасные**  
Указывает, что предоставлены всех запросов на передачу зон.  
**/securens**  
Указывает, что перенос предоставлены только на сервере, который указан в записи ресурса имени сервера (NS) для зоны.  
**/securelist**  
Указывает, что предоставлены передачи зоны только список серверов. Этот параметр следует указать IP-адрес или адреса, используемые главным сервером.  
**<SecurityIPaddresses>**  
Список IP-адресов, которые получать передачи зон с главного сервера. Этот параметр используется только с **/SecureList** параметра.  
** / nonotify | / Уведомлять | /notifylist <NotifyIPaddresses>**  
Указывает, что уведомление об изменениях отправляется только для определенных серверов-получателей:  
**/nonotify**  
Указывает, что уведомления не изменения отправляются с серверами-получателями.  
**/ уведомления**  
Отправка уведомлений об изменениях на всех серверах-получателях.  
**/notifylist**  
Отправка уведомлений об изменениях в список серверов. Эта команда следует указать IP-адрес или адреса, используемые главным сервером.  
**<NotifyIPaddresses>**  
Указывает IP-адреса сервера-получателя или нескольких серверов, какие изменения будут отправлены. Этот список используется только с **/notifylist** параметра. ### Примечания — использование **zoneresetsecondaries** команду на главном сервере, чтобы указать, как оно ответит запросов на передачу зон из серверов-получателей. ### Пример использования
<pre>dnscmd dnssvr1.contoso.com /zoneresetsecondaries test.contoso.com /noxfr /nonotify  
dnscmd dnssvr1.contoso.com /zoneresetsecondaries test.contoso.com /securelist 11.0.0.2</pre>  
### <a name="BKMK_29"></a>dnscmd /zoneresettype  
Изменяет тип зоны. ### Синтаксис ```  
dnscmd [<ServerName>] /zoneresettype <ZoneName> <Zonetype> [/ overwrite_mem | /overwrite_ds] ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный синтаксис локального компьютера, IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Определяет зону, на котором изменяется тип.  
**<Zonetype>**  
Указывает тип зоны, для создания. Каждый тип имеет различные необходимые параметры:  
**/ DsPrimary**  
Создает зоны, интегрированной в active directory.  
** / первичного/File <FileName>**  
Создает стандартной основной зоны.  
** или вторичных <MasterIPaddress> [,<MasterIPaddress>...] **  
Создает стандартный дополнительной зоны.  
** / stub <MasterIPaddress>[,<MasterIPaddress>...] / file <FileName>**  
Создает зона-заглушка с файловой поддержкой.  
** / dsstub <MasterIPaddress>[,<MasterIPaddress>...] **  
Создает зоны заглушки интегрированной с active directory.  
** / сервер пересылки <MasterIPaddress[,<MasterIPaddress>]... / file<FileName>**  
Указывает, что созданный зоны перенаправляет неразрешенные запросы на другой сервер DNS.  
**/dsforwarder**  
Указывает, что созданный интегрированной зоне active directory перенаправляет неразрешенные запросы на другой сервер DNS.  
**/overwrite_mem | /overwrite_ds**  
Указывает, как перезаписать существующие данные:  
**/overwrite_mem**  
Перезаписывает данные DNS из данных в Доменных службах Active Directory.  
**/overwrite_ds**  
Перезаписывает существующие данные в Доменных службах Active Directory. ### Примечания — пояса ввести в качестве **/dsforwarder** создается зона, которая выполняет условное перенаправление. ### Пример использования
<pre>dnscmd dnssvr1.contoso.com /zoneresettype test.contoso.com /primary /file test.contoso.com.dns  
dnscmd dnssvr1.contoso.com /zoneresettype second.contoso.com /secondary 10.0.0.2</pre>  
### <a name="BKMK_35"></a>dnscmd /zoneresume  
запускает указанной зоны, которая ранее была приостановлена. ### Синтаксис ```  
dnscmd <ServerName> /zoneresume <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, чтобы возобновить. ### Примечания — эту операцию можно использовать для отмены [zonepause](#BKMK_27) операции. ### Пример использования `dnscmd dnssvr1.contoso.com /zoneresume test.contoso.com`  
### <a name="BKMK_36"></a>dnscmd /zoneupdatefromds  
Обновляет указанный интегрированной зоне active directory из AD DS. ### Синтаксис ```  
dnscmd <ServerName> /zoneupdatefromds <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, для обновления. ### Примечания — active directory интегрированной зоны обновления по умолчанию каждые пять минут. Чтобы изменить этот параметр, используйте **dnscmd config dspollinginterval** команды. ### Пример использования `dnscmd dnssvr1.contoso.com /zoneupdatefromds`  
### <a name="BKMK_37"></a>dnscmd /zonewriteback  
Проверяет память сервера DNS для изменения, относящиеся к определенной зоне и записывает их в постоянное хранилище. ### Синтаксис ```  
dnscmd <ServerName> /zonewriteback <ZoneName> ```  
#### Parameters  
**<ServerName>**  
Указывает DNS-сервер для управления, представленный IP-адрес, полное доменное имя или имя узла. Если этот параметр опущен, используется локальный сервер.  
**<ZoneName>**  
Указывает имя зоны, для обновления. ### Примечания — это операция уровня зоны. Вы можете обновить все зоны на DNS-сервер с [writebackfiles](#BKMK_21) операции. ### Пример использования `dnscmd dnssvr1.contoso.com /zonewriteback test.contoso.com`  
