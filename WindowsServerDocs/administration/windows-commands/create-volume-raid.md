---
title: Создание тома RAID
description: Справочный раздел по команде Create Volume RAID, который создает том RAID-5, используя три или более указанных динамических диска.
ms.prod: windows-server
ms.technology: manage-windows-commands
ms.topic: article
ms.assetid: 9f257950-9240-4d5f-9537-8ad653d48ebf
author: coreyp-at-msft
ms.author: coreyp
manager: dongill
ms.date: 10/16/2017
ms.openlocfilehash: 1c773cf4412640759910e8c127a95f7cc34581d4
ms.sourcegitcommit: fad2ba64bbc13763772e21ed3eabd010f6a5da34
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/09/2020
ms.locfileid: "82993230"
---
# <a name="create-volume-raid"></a>Создание тома RAID

> Область применения: Windows Server (половина ежегодного канала), Windows Server 2019, Windows Server 2016, Windows Server 2012 R2, Windows Server 2012

Создает том RAID-5, используя три или более указанных динамических диска. После создания тома фокус автоматически переместится на новый том.

## <a name="syntax"></a>Синтаксис

```
create volume raid [size=<n>] disk=<n>,<n>,<n>[,<n>,...] [align=<n>] [noerr]
```

### <a name="parameters"></a>Параметры

| Параметр | Описание |
| --------- | ----------- |
| Размер =`<n>` | Объем дискового пространства в мегабайтах (МБ), который будет занимать том на каждом диске. Если размер не указан, будет создан самый крупный том RAID-5. Диск с наименьшим доступным непрерывным свободным пространством определяет размер тома RAID-5 и тот же объем дискового пространства, выделенный для каждого диска. Фактический объем свободного дискового пространства на томе RAID-5 меньше, чем общий объем дискового пространства, так как для четности требуется некоторое дисковое пространство. |
| диск =`<n>,<n>,<n>[,<n>,...]` | Динамические диски, на которых создается том RAID-5. Для создания тома RAID-5 необходимо по крайней мере три динамических диска. Объем пространства `size=<n>` , равный, выделяется на каждом диске. |
| Выровняйте =`<n>` | Выравнивает все экстенты томов по ближайшей границе выравнивания. Обычно используется с массивами LUN аппаратного RAID-массива для повышения производительности. `<n>`количество килобайтов (КБ) от начала диска до ближайшей границы выравнивания. |
| Noerr | Только для сценариев. При возникновении ошибки DiskPart продолжит обрабатывать команды, как если бы ошибка не возникала. Без этого параметра ошибка приводит к выходу из программы DiskPart с кодом ошибки. |

## <a name="examples"></a>Примеры

Чтобы создать том RAID-5 размером 1000 МБ, используя диски 1, 2 и 3, введите:

```
create volume raid size=1000 disk=1,2,3
```

## <a name="additional-references"></a>Дополнительные ссылки

- [Условные обозначения синтаксиса команд командной строки](command-line-syntax-key.md)

- [создать команду](create.md)
