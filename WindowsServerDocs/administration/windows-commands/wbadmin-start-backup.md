---
title: начало архивации WBADMIN
description: 'Раздел Windows команды для ***- '
ms.custom: na
ms.prod: windows-server-threshold
ms.reviewer: na
ms.suite: na
ms.technology: manage-windows-commands
ms.tgt_pltfrm: na
ms.topic: article
ms.assetid: 56f3e752-d99a-4c3d-8e97-10303c37dd78
author: coreyp-at-msft
ms.author: coreyp
manager: dongill
ms.date: 10/16/2017
ms.openlocfilehash: 2ac602506960b92333750e7a37692c44c92aae22
ms.sourcegitcommit: eaf071249b6eb6b1a758b38579a2d87710abfb54
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/31/2019
ms.locfileid: "66440274"
---
# <a name="wbadmin-start-backup"></a>начало архивации WBADMIN



Создает резервную копию, используя указанные параметры. Если параметры не указаны, вы создали запланированное ежедневное резервное копирование подкоманды создает резервную копию, используя параметры для запланированного резервного копирования. Если указаны параметры, он создает резервную копию службы теневого копирования томов (VSS) и не будут обновляться истории файлов, которые были созданы резервные копии.

Чтобы создать одну резервную копию с подкоманды, необходимо быть членом **операторы архива** группы или **Администраторы** группа, или должна была быть делегированы соответствующие разрешения. Кроме того, необходимо запустить **wbadmin** из командной строки с повышенными правами. (Чтобы открыть командную строку, щелкните правой кнопкой мыши **командной** и нажмите кнопку **Запуск от имени администратора**.)

Примеры по использованию этого, см. в разделе [примеры](#BKMK_examples).

## <a name="syntax"></a>Синтаксис

Синтаксис для Windows ° Vista и Windows Server 2008:
```
wbadmin start backup
[-backupTarget:{<BackupTargetLocation> | <TargetNetworkShare>}]
[-include:<VolumesToInclude>]
[-allCritical]
[-noVerify]
[-user:<UserName>]
[-password:<Password>]
[-noinheritAcl]
[-vssFull]
[-quiet]
```
Синтаксис для Windows ° 7 и Windows Server 2008 R2 и более поздних версий:
```
Wbadmin start backup
[-backupTarget:{<BackupTargetLocation> | <TargetNetworkShare>}]
[-include:<ItemsToInclude>]
[-nonRecurseInclude:<ItemsToInclude>]
[-exclude:<ItemsToExclude>]
[-nonRecurseExclude:<ItemsToExclude>]
[-allCritical]
[-systemState]
[-noVerify]
[-user:<UserName>]
[-password:<Password>]
[-noInheritAcl]
[-vssFull | -vssCopy] 
[-quiet]
```

## <a name="parameters"></a>Параметры

|Параметр|Описание|
|---------|-----------|
|-backupTarget|Задает место хранения для этой резервной копии. Требуется буква жесткого диска ("f:"), путь на основе GUID тома в формат \\ \\? \Volume{GUID}, или путем универсальными именами (UNC) к удаленной общей папке (\\\\\<servername > \<sharename >\). По умолчанию, резервная копия будет сохранен в: \\ \\ <servername> \<sharename >\** WindowsImageBackup **\\<ComputerBackedUp>\.</br>Внимание! При сохранении резервной копии в удаленную общую папку резервной копии будут перезаписаны при использовании той же папке для архивации том же компьютере. Кроме того Если происходит сбой операции резервного копирования, может оказаться без резервных копий, так как более ранней резервной копии будут перезаписаны, но новая будет непригодным для использования. Этого можно избежать путем создания вложенных папок в удаленную общую папку для организации резервных копий. После этого, вложенные папки потребуется дважды места родительской папки.|
|-включить|Для Windows ° Vista и Windows Server 2008 задает разделенный запятыми список букв дисков томов, точек подключения томов или имен томов на основе GUID для включения в резервную копию. Этот параметр следует использовать только тогда, когда **- backupTarget** используется параметр.</br>Для Windows ° 7 и Windows Server 2008 R2 и более поздних версиях указывает разделенный запятыми список элементов, включаемых в резервную копию. Можно включить несколько файлов, папок или томов. Пути тома можно указать при помощи букв дисков томов, точек подключения томов или имен томов на основе GUID. Если вы используете имя тома на основе GUID, он должен быть завершен обратную косую черту (\\). Можно использовать подстановочный знак (\*) в имени файла, указывая путь к файлу. Следует использовать только тогда, когда **- backupTarget** используется параметр.|
|-исключение|Для Windows ° 7 и Windows Server 2008 R2 и более поздних версиях Указывает список с разделителями запятыми элементы, исключаемые из резервной копии. Вы можете исключить файлы, папки или тома. Пути тома можно указать при помощи букв дисков томов, точек подключения томов или имен томов на основе GUID. Если вы используете имя тома на основе GUID, он должен быть завершен обратную косую черту (\\). Можно использовать подстановочный знак (\*) в имени файла, указывая путь к файлу. Следует использовать только тогда, когда **- backupTarget** используется параметр.|
|-nonRecurseInclude|Для Windows ° 7 и Windows Server 2008 R2 и более поздних версиях указывает нерекурсивный, разделенный запятыми список элементов, включаемых в резервную копию. Можно включить несколько файлов, папок или томов. Пути тома можно указать при помощи букв дисков томов, точек подключения томов или имен томов на основе GUID. Если вы используете имя тома на основе GUID, он должен быть завершен обратную косую черту (\\). Можно использовать подстановочный знак (\*) в имени файла, указывая путь к файлу. Следует использовать только тогда, когда **- backupTarget** используется параметр.|
|-nonRecurseExclude|Для Windows ° 7 и Windows Server 2008 R2 и более поздних версиях указывает нерекурсивный, разделенный запятыми список элементов для исключения из резервной копии. Вы можете исключить файлы, папки или тома. Пути тома можно указать при помощи букв дисков томов, точек подключения томов или имен томов на основе GUID. Если вы используете имя тома на основе GUID, он должен быть завершен обратную косую черту (\\). Можно использовать подстановочный знак (\*) в имени файла, указывая путь к файлу. Следует использовать только тогда, когда **- backupTarget** используется параметр.|
|-allCritical|Указывает, что все критические тома (тома, содержащие состояние операционной системы) будут включены в резервные копии. Этот параметр полезен при создании резервной копии для восстановления исходного состояния системы. Он должен использоваться только тогда, когда **- backupTarget** будет указано, в противном случае команда завершится ошибкой. Можно использовать с **-включают** параметр.</br>Совет. Тома для резервного копирования критические тома может быть локальный диск, но не может быть любой из томов, которые включаются в резервную копию.|
|-systemState|Для Windows ° 7 и Windows Server 2008 R2 и более поздних версиях, создает резервную копию, включающую состояние системы, помимо других элементов, указанных с **-включают** параметра. Состояние системы содержит файлы для загрузки (Boot.ini, NDTLDR, NTDetect.com), реестра Windows, включая параметры COM, SYSVOL (групповые политики и сценарии входа в систему), Active Directory и NTDS. Информационного дерева КАТАЛОГА на контроллерах домена и, если установлена служба сертификатов, сертификат Store. Если сервер имеет роли веб-сервера, который установлен, метакаталог IIS будут включены. Если сервер является частью кластера, сведения о службе кластера также будут включены.|
|-backupTarget место хранения|Указывает, что резервные копии, сохраненные на съемный носитель (например, DVD-диска) не проверяются для ошибки. Если вы не используете этот параметр, наличие ошибок проверяются на съемном носителе будут сохранены резервные копии.|
|-пользователя|Если резервная копия сохраняется в удаленную общую папку, указывает имя пользователя с разрешением на запись в папку.|
|-пароль|Указывает пароль для имени пользователя, который предоставляется в параметре **-пользователь**.|
|-noInheritAcl|Применяет разрешения списка управления Доступом управления доступом, которые соответствуют учетным данным, предоставленным с **-пользователь** и **-пароль** параметров \\ \\ \< ServerName >\<sharename > \WindowsImageBackup\<ComputerBackedUp > \ (папку, содержащую резервного копирования). Для доступа к резервной копии более поздней версии, необходимо использовать эти учетные данные или быть членом группы "Администраторы" или в группу операторов резервного копирования, на компьютере с общей папке. Если **- noInheritAcl** — не используется, разрешения списка управления Доступом из удаленной общей папки применяются к \<ComputerBackedUp > папки по умолчанию, поэтому, необходимая любому пользователю с доступом к удаленной общей папке можно получить доступ к резервной копии.|
|-vssFull|Выполняет полное резервное копирование с помощью службы для теневого копирования томов (VSS). Резервное копирование всех файлов, каждый файл журнала будет обновлена с учетом его резервная копия, что журналы предыдущие резервные копии могут быть усечены. Если этот параметр не используется **архивации wbadmin start** делает резервной копии, но историю файлов резервного копирования не обновляется.</br>Осторожно. Не используйте этот параметр, если при использовании продукта отличных от Windows Server Backup для резервного копирования приложений, которые находятся на томах, включенные в текущую резервную копию. Это поэтому может потенциально нарушить увеличения, разностной резервной копии или других видов резервных копий, которые этот сторонний продукт резервного копирования создается в том случае, так как журнал, который они полагаетесь на чтобы определить, какой объем данных для резервного копирования могут отсутствовать и они могут выполнять полную резервного копирования без необходимости.|
|-vssCopy|Для Windows 7 и Windows Server 2008 R2 и более поздней версии, выполняет резервное копирование копии с помощью платформы VSS. Резервное копирование всех файлов, но историю файлов, архивируются не обновляется, поэтому вы можете сохранить все данные на какие файлы там, где измененные, удаленные и т. д., а также файлы журнала приложений. Используя этот тип резервного копирования не влияет на последовательность добавочные и разностные резервные копии, которые могут происходить независимо от этого резервного копирования. Это значение по умолчанию.</br>Предупреждение: Копирование не может использоваться для добавочные или разностные резервные копии или восстановления.|
|-quiet|Запускает подкоманды без вывода сообщений для пользователя.|

## <a name="BKMK_examples"></a>Примеры

В следующих примерах показывается как **архивации wbadmin start** команда может использоваться в различных сценариях резервного копирования:

Сценарий #1
- Создайте резервную копию тома e:, d:\mountpoint, и \\ \\? \Volume{cc566d14-4410-11d9-9d93-806e6f6e6963}
- Сохранить резервную копию для тома f:
  ```
  wbadmin start backup -backupTarget:f: -include:e:,d:\mountpoint,\\?\Volume{cc566d14-44a0-11d9-9d93-806e6f6e6963}\
  ```
  # Сценария 2
- Выполните резервное копирование одноразовый *f:\folder1* и *h:\folder2* том *d:* .
- Резервное копирование состояния системы
- Создайте резервную копию таким образом, чтобы не влияет на запланированное разностной резервной копии.
  ```
  wbadmin start backup –backupTarget:d: -include:g\folder1,h:\folder2 –systemstate -vsscopy
  ```
  Сценарий #3
- Выполните резервное копирование одноразовый *d:\folder1* , необходимо архивировать нерекурсивно.
- Создать резервную копию папки к сетевому расположению  *\\ \\backupshare\backup1*
- Ограничение доступа к резервной копии к членам **Администраторы** или **операторы архива** группы.
  ```
  wbadmin start backup –backupTarget: \\backupshare\backup1 -noinheritacl -nonrecurseinclude:d:\folder1
  ```

#### <a name="additional-references"></a>Дополнительная справка

-   [Условные обозначения синтаксиса команд командной строки](command-line-syntax-key.md)
-   [WBADMIN](wbadmin.md)
