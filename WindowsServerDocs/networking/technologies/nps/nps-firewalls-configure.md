---
title: Настройка брандмауэров для RADIUS-трафика
description: В этом разделе приводятся общие сведения о настройке брандмауэров для разрешения трафика RADIUS для сервера политики сети в Windows Server 2016.
manager: brianlic
ms.prod: windows-server
ms.technology: networking
ms.topic: article
ms.assetid: 58cca2b2-4ef3-4a09-a614-8bdc08d24f15
ms.author: pashort
author: shortpatti
ms.openlocfilehash: eb7f5c495fa09aed584ef37d668d1fa232d7a497
ms.sourcegitcommit: 6aff3d88ff22ea141a6ea6572a5ad8dd6321f199
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 09/27/2019
ms.locfileid: "71405442"
---
# <a name="configure-firewalls-for-radius-traffic"></a>Настройка брандмауэров для RADIUS-трафика

>Относится к: Windows Server (Semi-Annual Channel), Windows Server 2016

Брандмауэры можно настроить для разрешения или блокировки типов IP-трафика на компьютере или устройстве, на котором работает брандмауэр. Если брандмауэры не настроены должным образом, чтобы разрешить трафик RADIUS между клиентами RADIUS, RADIUS-прокси и серверами RADIUS, проверка подлинности доступа к сети может завершиться сбоем, предотвращая доступ пользователей к сетевым ресурсам. 

Может потребоваться настроить два типа брандмауэров, чтобы разрешить трафик RADIUS.

- Брандмауэр защитника Windows в повышенной безопасности на локальном сервере, на котором выполняется сервер политики сети (NPS).
- Брандмауэры, работающие на других компьютерах или устройствах.

## <a name="windows-firewall-on-the-local-nps"></a>Брандмауэр Windows в локальном NPS

По умолчанию сервер политики сети отправляет и получает трафик RADIUS с помощью протокола датаграммы пользователя \(UDP @ no__t-1 Ports 1812, 1813, 1645 и 1646. Брандмауэр защитника Windows на сервере политики сети автоматически настраивается с исключениями во время установки NPS, чтобы разрешить отправку и получение трафика RADIUS.

Поэтому, если используются UDP-порты по умолчанию, не нужно изменять конфигурацию брандмауэра защитника Windows, чтобы разрешить трафик RADIUS в НПСС и обратно.

В некоторых случаях может потребоваться изменить порты, используемые NPS для трафика RADIUS. Если сервер политики сети и серверы доступа к сети настроены для отправки и получения RADIUS-трафика на портах, отличных от параметров по умолчанию, необходимо выполнить следующие действия.

- Удалите исключения, которые разрешают трафик RADIUS на портах по умолчанию.
- Создайте новые исключения, разрешающие трафик RADIUS на новых портах.

Дополнительные сведения см. в разделе [Настройка данных UDP-портов NPS](nps-udp-ports-configure.md).

## <a name="other-firewalls"></a>Другие брандмауэры

В наиболее распространенной конфигурации брандмауэр подключен к Интернету, а NPS — это ресурс интрасети, подключенный к сети периметра.

Для доступа к контроллеру домена в интрасети сервер политики сети может иметь следующее:

- Интерфейс в сети периметра и интерфейс в интрасети (IP-маршрутизация не включена). 
- Единый интерфейс в сети периметра. В этой конфигурации NPS взаимодействует с контроллерами домена через другой брандмауэр, который подключает сеть периметра к интрасети.

## <a name="configuring-the-internet-firewall"></a>Настройка брандмауэра Интернета

Брандмауэр, подключенный к Интернету, должен быть настроен с входными и выходными фильтрами в интерфейсе Интернета \(and (необязательно) с помощью интерфейса периметра сети @ no__t-1, чтобы разрешить перенаправление сообщений RADIUS между NPS и RADIUS. клиенты или прокси-серверы в Интернете. Можно использовать дополнительные фильтры, чтобы разрешить передачу трафика на веб-серверы, VPN-серверы и другие типы серверов в сети периметра.

Отдельные фильтры входящих и исходящих пакетов можно настроить для интерфейса Интернета и интерфейса сети периметра.

### <a name="configure-input-filters-on-the-internet-interface"></a>Настройка фильтров входа для интерфейса Интернета

Настройте следующие фильтры входящих пакетов в интерфейсе Интернета брандмауэра, чтобы разрешить следующие типы трафика:

- Конечный IP-адрес интерфейса сети периметра и порт назначения UDP 1812 (0x714) NPS.  Этот фильтр разрешает трафик проверки подлинности RADIUS от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2865. Если вы используете другой порт, замените номер этого порта 1812.
- Конечный IP-адрес интерфейса сети периметра и порт назначения UDP 1813 (0x715) NPS. Этот фильтр разрешает RADIUS-трафик учета от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2866. Если вы используете другой порт, замените номер этого порта 1813.
- \(Optional @ no__t-1 IP-адрес назначения интерфейса сети периметра и порт назначения UDP 1645 \(0x66D @ no__t-3 сервера политики сети. Этот фильтр разрешает трафик проверки подлинности RADIUS от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт, используемый старыми клиентами RADIUS.
- \(Optional @ no__t-1 IP-адрес назначения интерфейса сети периметра и порт назначения UDP 1646 \(0x66E @ no__t-3 сервера политики сети. Этот фильтр разрешает RADIUS-трафик учета от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт, используемый старыми клиентами RADIUS.

### <a name="configure-output-filters-on-the-internet-interface"></a>Настройка фильтров вывода для интерфейса Интернета

Настройте следующие фильтры вывода в интерфейсе Интернета брандмауэра, чтобы разрешить следующие типы трафика:

- Исходный IP-адрес интерфейса сети периметра и UDP-порт источника 1812 (0x714) NPS. Этот фильтр разрешает трафик проверки подлинности RADIUS от NPS к клиентам RADIUS, основанным на Интернете. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2865. Если вы используете другой порт, замените номер этого порта 1812.
- Исходный IP-адрес интерфейса сети периметра и UDP-порт источника 1813 (0x715) NPS. Этот фильтр разрешает RADIUS-трафик с сервера политики сети к клиентам RADIUS на основе Интернета. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2866. Если вы используете другой порт, замените номер этого порта 1813.
- \(Optional @ no__t-1 исходный IP-адрес интерфейса сети периметра и порт источника UDP 1645 \(0x66D @ no__t-3 сервера политики сети. Этот фильтр разрешает трафик проверки подлинности RADIUS от NPS к клиентам RADIUS, основанным на Интернете. Это UDP-порт, используемый старыми клиентами RADIUS.
- \(Optional @ no__t-1 исходный IP-адрес интерфейса сети периметра и порт источника UDP 1646 \(0x66E @ no__t-3 сервера политики сети. Этот фильтр разрешает RADIUS-трафик с сервера политики сети к клиентам RADIUS на основе Интернета. Это UDP-порт, используемый старыми клиентами RADIUS.

### <a name="configure-input-filters-on-the-perimeter-network-interface"></a>Настройка фильтров входа для интерфейса сети периметра

Настройте следующие фильтры входа в интерфейсе сети периметра брандмауэра, чтобы разрешить следующие типы трафика:

- Исходный IP-адрес интерфейса сети периметра и UDP-порт источника 1812 (0x714) NPS. Этот фильтр разрешает трафик проверки подлинности RADIUS от NPS к клиентам RADIUS, основанным на Интернете. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2865. Если вы используете другой порт, замените номер этого порта 1812.
- Исходный IP-адрес интерфейса сети периметра и UDP-порт источника 1813 (0x715) NPS. Этот фильтр разрешает RADIUS-трафик с сервера политики сети к клиентам RADIUS на основе Интернета. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2866. Если вы используете другой порт, замените номер этого порта 1813.
- \(Optional @ no__t-1 исходный IP-адрес интерфейса сети периметра и порт источника UDP 1645 \(0x66D @ no__t-3 сервера политики сети. Этот фильтр разрешает трафик проверки подлинности RADIUS от NPS к клиентам RADIUS, основанным на Интернете. Это UDP-порт, используемый старыми клиентами RADIUS.
- \(Optional @ no__t-1 исходный IP-адрес интерфейса сети периметра и порт источника UDP 1646 \(0x66E @ no__t-3 сервера политики сети. Этот фильтр разрешает RADIUS-трафик с сервера политики сети к клиентам RADIUS на основе Интернета. Это UDP-порт, используемый старыми клиентами RADIUS.

### <a name="configure-output-filters-on-the-perimeter-network-interface"></a>Настройка фильтров вывода в интерфейсе сети периметра

Настройте следующие фильтры исходящих пакетов в интерфейсе сети периметра брандмауэра, чтобы разрешить следующие типы трафика:

- Конечный IP-адрес интерфейса сети периметра и порт назначения UDP 1812 (0x714) NPS. Этот фильтр разрешает трафик проверки подлинности RADIUS от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2865. Если вы используете другой порт, замените номер этого порта 1812.
- Конечный IP-адрес интерфейса сети периметра и порт назначения UDP 1813 (0x715) NPS. Этот фильтр разрешает RADIUS-трафик учета от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт по умолчанию, используемый NPS, как определено в RFC 2866. Если вы используете другой порт, замените номер этого порта 1813.
- \(Optional @ no__t-1 IP-адрес назначения интерфейса сети периметра и порт назначения UDP 1645 \(0x66D @ no__t-3 сервера политики сети. Этот фильтр разрешает трафик проверки подлинности RADIUS от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт, используемый старыми клиентами RADIUS.
- \(Optional @ no__t-1 IP-адрес назначения интерфейса сети периметра и порт назначения UDP 1646 \(0x66E @ no__t-3 сервера политики сети. Этот фильтр разрешает RADIUS-трафик учета от клиентов RADIUS на основе Интернета к NPS. Это UDP-порт, используемый старыми клиентами RADIUS.

Для повышения безопасности можно использовать IP-адреса каждого клиента RADIUS, который отправляет пакеты через брандмауэр, чтобы определить фильтры для трафика между клиентом и IP-адресом NPS в сети периметра.

### <a name="filters-on-the-perimeter-network-interface"></a>Фильтры в интерфейсе сети периметра

Настройте следующие фильтры входящих пакетов в интерфейсе сети периметра брандмауэра интрасети, чтобы разрешить следующие типы трафика:

- Исходный IP-адрес интерфейса периметра сети NPS. Этот фильтр разрешает трафик из NPS в сети периметра.

Настройте следующие фильтры вывода в интерфейсе сети периметра брандмауэра интрасети, чтобы разрешить следующие типы трафика:

- Конечный IP-адрес интерфейса периметра сети NPS. Этот фильтр разрешает трафик к NPS в сети периметра.

### <a name="filters-on-the-intranet-interface"></a>Фильтры в интерфейсе интрасети

Настройте следующие фильтры входа в интерфейсе интрасети брандмауэра, чтобы разрешить следующие типы трафика:

- Конечный IP-адрес интерфейса периметра сети NPS. Этот фильтр разрешает трафик к NPS в сети периметра.

Настройте следующие фильтры исходящих пакетов в интерфейсе интрасети брандмауэра, чтобы разрешить следующие типы трафика:

- Исходный IP-адрес интерфейса периметра сети NPS. Этот фильтр разрешает трафик из NPS в сети периметра.


Дополнительные сведения об управлении NPS см. в разделе [Управление сервером политики сети](nps-manage-top.md).

Дополнительные сведения о NPS см. в разделе [сервер политики сети (NPS)](nps-top.md).




